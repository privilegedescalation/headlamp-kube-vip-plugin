name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_call:
  workflow_dispatch:

jobs:
  ci:
    uses: privilegedescalation/.github/.github/workflows/plugin-ci.yaml@main
