name: CI

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]
  workflow_call:
  workflow_dispatch:

jobs:
  ci:
    uses: privilegedescalation/.github/.github/workflows/plugin-ci.yaml@main
